<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardCudDao">
    <insert id="saveBoard" parameterType="com.boardapp.boardapi.board.model.Board">
        INSERT
        INTO board(
            board_title,
            board_contents,
            write_id,
            write_date
        ) VALUES (
            #{boardTitle},
            #{boardContents},
            #{writeId},
            #{writeDate}
        )
    </insert>

    <update id="updateBoard" parameterType="com.boardapp.boardapi.board.model.Board">
        UPDATE board 
        SET board_title = #{boardTitle},
            board_contents = #{boardContents},
            modify_id = #{modifyId},
            modify_date = #{modifyDate}
        WHERE board_id = #{boardId}
    </update>
    
    <delete id="deleteBoard" parameterType="Long">
        DELETE
        FROM board
        WHERE board_id = #{boardId}
    </delete>
</mapper>